// Profile / Earnings screen — Demo Screen Builder populates this.
// Phone frame bottom tab: "Profile" (person icon)
// Shows: shop banner + star rating + earnings summary + listing grid (buyer-facing storefront view)

export default function ProfilePage() {
  return (
    <div className="p-4">
      <h1 className="text-xl font-bold tracking-tight">My Shop</h1>
      <p className="text-sm text-muted-foreground mt-1">Placeholder — Demo Screen Builder.</p>
    </div>
  );
}
